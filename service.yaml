apiVersion: v1
kind: Service
metadata:
  name: swiggy-service
  labels:
    app: swiggy
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
  selector:
    app: swiggy